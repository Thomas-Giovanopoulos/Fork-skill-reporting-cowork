# CDC — Pipeline de reporting Rhétorès : du skill-agrégateur au skill-consommateur

**Version 4 — 27/07/2026.** Amende la v3 du même jour (v1 du 22/07 et v2 conservées comme références figées).
Rédigé en atelier (Thomas × Cowork). Document de référence pour les amendements au process.

> **Amendée par la v5 du 29/07** (`CDC_v5_2026-07-29.md`) : Excel coupé du pipeline (D45–D46),
> réemploi de l'`excel_to_store` de validation_app (D47), convergence des forme-stores (D48),
> provenance d'extraction (D49), chantier L re-scopé, chantier D abandonné, O3 clos.

**Apports de la v4** : l'étude empirique de signature des gabarits (11 relevés, 11 sous-agents) et le
salvage du contexte Gronier (31 relevés déjà parsés, référentiels de fait du moteur).

---

## Section 0 — Ce qui change depuis la v3

La v3 posait le fork, le lecteur de forme-store en colonne vertébrale, et un chantier K « table des
couples partenaires ». **L'étude de signature montre que K recouvre en réalité deux tables de natures
distinctes**, et le salvage Gronier corrige une lecture du code.

| | Objet | Nature du changement |
|---|---|---|
| 1 | **Scission de K** | K devient le **référentiel des acteurs** (qui est cette entité, quel rôle). Un nouveau chantier **N** porte le **référentiel des gabarits** (de quel template ce document relève). Deux tables, une gouvernance (D22). |
| 2 | **Trois référentiels, un circuit** | Acteurs (K), gabarits (N), ISIN (E) partagent `meta.reference_tables`, le circuit de proposition et la cotation de `confidence`. E5 s'élargit (D23). |
| 3 | **Identification par le contenu** | L'émetteur d'un document ne se déduit ni du nom de fichier, ni du partenaire commercial. Identification par signature uniquement (D24). |
| 4 | **Invariant de contrôle par gabarit** | Chaque profil porte son checksum interne (Σ lignes = tel total). Auto-validation de l'extraction sans humain → moins de pauses. Nouveau mécanisme, sert B3 (D25). |
| 5 | **Correction de lecture — `depo == "NC"`** | La v3 le qualifiait de « faux silencieux ». Le salvage montre que **NC = « non communicable » est une règle réglementaire documentée** (triangle de sécurité LU), pas un bug. Le défaut réel est l'homonymie entre « non communicable » et « non renseigné » (T5, D26). |
| 6 | **Seed des référentiels disponible** | 7 profils de gabarit / 11 documents (étude) + les acteurs et pièges des 31 relevés Gronier. Le cold-start est évité sans travail neuf (D27). |
| 7 | **Donnée de cadence réelle** | Gronier : 31 relevés extraits en **3 vagues de ~6 min**. Le parallélisme est plafonné par vague — révise l'hypothèse de gain (O10). |

---

## Section 1 — Résumé de la discussion (amendé)

### 1.1 Le constat fondateur

*Inchangé (v3).* Le skill est né avant le datahub. Par nature c'est un **consommateur** ; faute
d'infra, l'agrégation s'est installée dedans. C'est assumé et temporaire : on agrège ici, en visant
une forme interne strictement compatible, pour que l'agrégation soit un jour **débranchée** sans que
le reste s'en aperçoive.

*Lecture v4* : l'étude de signature confirme que le module d'agrégation mérite d'être **rigidifié**
plutôt qu'accéléré. Le gain de temps est un effet de bord ; l'objectif est la réduction des reprises.

### 1.2 Le rôle de l'Excel — rétrogradé trois fois

*Inchangé (v3).* Plus source de vérité unique ; projection lossy ; l'artefact prêt-pour-datahub est
le JSON forme-store + les diffs ; et depuis D20, l'Excel de transition sort du chemin de rendu — il
ne subsiste que comme surface de rectification CGP.

### 1.3 L'architecture cible du skill

```
  ┌─ Excel de structure (CGP) ──┐
  ├─ PDFs relevés partenaires ──┤        ┌──────────────────────────────┐
  ├─ XML PENELOP ───────────────┤        │  RÉFÉRENTIELS (meta.         │
  └─ API partenaire (≥3 mois) ──┘        │  reference_tables)           │
        │                                │   • acteurs      [K]         │
        │  ┌─────────────────────────────┤   • gabarits     [N]         │
        │  │  identification du gabarit  │   • ISIN         [E]         │
        │  │  (signature 3 couches)      └──────────────────────────────┘
        │  │        │                                    ▲
        │  │   match│drift ──► ré-analyse ──► proposition de profil
        │  ▼        ▼                          (appliquée à CE run, arbitrée après)
  subagents extraction (1/document, parallèle) ──► diffs
        │     primés par le profil : ancrage, pièges, champs publiés
        ▼
  invariant de contrôle du gabarit ──► auto-validation (Σ lignes = total)
        ▼
  réconciliation vs structure ──► AskUser sur écarts / low confidence
        ▼                          (confidence cotée depuis K et N, pas au jugement)
  apply ──► dict client FORME-STORE  ◄────── mode (b) D6 : {client}.json direct
        │      (= le pivot interne)
        ├──► store_to_manifest ─────────────► manifeste (blocs_enabled)   [J]
        ├──► lecteur de store ──────────────► données de rendu            [L]
        │         └──► p2_fill (bank/ figé) ──► HTML (livrable)
        └──► writer (json_to_excel vendoré) ──► Excel de transition (latéral)
```

**La couture** : tout ce qui précède le dict forme-store est le *module d'agrégation amovible*.
Tout ce qui suit ne connaît que ce dict.

**Ce que la v4 ajoute au schéma** : la boucle d'identification/self-healing en amont de l'extraction,
et les trois référentiels comme dépendance transverse (et non plus comme un chantier isolé).

### 1.4 Alignement sur l'infra (rapports Code)

*Inchangé (v3).* `validation_app` : hub d'ingestion multi-source, **on n'y touche pas dans ce cycle**.
Store Postgres : `clients.meta` (JSONB : client, reporting, **`reference_tables`**, valuations,
courbe_performance), `entries`, `ignored`. `json_to_excel.py` à vendorer. **Le contrat de diff est le
point de contact à ne pas casser pendant le fork.**

*Précision v4* : `reference_tables` étant déjà nommé côté infra, les **trois** tables y logent sans
inventer de concept. Ne reste à deviner que la forme d'une ligne — et la forme d'un profil de gabarit
est désormais dérivée de 11 cas réels, pas d'une intuition (cf. 1.10).

### 1.5 Usage et positionnement

*Inchangé.* Le chat est l'atelier ; la génération de routine est déterministe ; Claude est
irremplaçable en amont (extraction) et en latéral (raffinement). Accès direct à la base off-limits.

### 1.6 Le tri des blocs

*Inchangé (v3).* `blocs_enabled` mélange **intention / faisabilité / pertinence** dans un littéral de
dict ; la faisabilité est déduite d'une topologie d'onglets que A1 supprime ; la décision est prise
après le writer. D6 tranche : les deux modes convergent sur le forme-store, et en mode (b) il n'y a
aucun Excel. **Le store répond « peut-on ? », pas « veut-on ? ».**

### 1.7 Référentiels — trois tables, une gouvernance *(refondu en v4)*

La v3 parlait d'« une table des couples partenaires ». L'étude de signature montre que deux questions
distinctes étaient confondues :

| Table | Question | Contenu | Chantier |
|---|---|---|---|
| **A — Acteurs** | *Qui est cette entité, quel rôle joue-t-elle ?* | code canonique, **rôle** (assureur / dépositaire / gérant / plateforme-grossiste), alias verbatim des PDF, domiciliation, **successions datées**, couples admissibles | **K** |
| **B — Gabarits** | *De quel template ce document relève-t-il ?* | signature 3 couches, hints d'extraction, **champs publiés**, **invariant de contrôle**, périodicité et son token discriminant | **N** *(nouveau)* |
| **C — ISIN** | *Quelle est la classe/géo/SRI de cette ligne ?* | isin → label, class_code, geo_code, sri, source, confidence | **E** |

**Le lien** : le champ `emetteur` d'un profil de gabarit (B) est une **clé étrangère** vers un code
canonique d'acteur (A). Elles ne se substituent pas, elles se chaînent.

**Ce qui reste vrai de la v3.** La table est déjà là en version dégradée et éclatée dans
`p1_engine/p2_fill.py` : `DEPO_COLORS` (6 partenaires en dur, clé = libellé), `ENV_COLORS` (7
enveloppes), `envelope(nature, depo)`. Le couple *(assureur, dépositaire)* est lu ligne à ligne mais
jamais confronté à un référentiel. Constat aggravé par le salvage : **aucun** des partenaires réels de
Gronier (Nortia, Cardif, CIC, Quintet, Thaler, Intesa) n'est dans `DEPO_COLORS` — ils reçoivent une
couleur `FALLBACK` attribuée dans l'ordre de première rencontre, donc **non déterministe d'un run à
l'autre**. C'est un défaut de reproductibilité, pas seulement d'exactitude.

**Sur le routage**, D14 tient : le relevé énonce lui-même qu'il porte sur une poche du contrat X. Le
document transporte la jointure, le couple n'a pas à porter l'identité de la poche → les poches
restent en `attributes.pockets`. Deux réserves à encoder : la **référence contrat est la clé primaire,
la table des acteurs le validateur de repli** ; et c'est le **libellé de poche qui dérive** alors qu'il
sert de clé de regroupement (colonne 16) → identifiant stable requis (D16).

**Le circuit est unique pour les trois tables** : élément inconnu → `unrecognized_data` + proposition
d'ajout à arbitrer, jamais d'écriture directe pendant un run. C'est le mécanisme E3, généralisé (E5).

### 1.8 Le fork : pourquoi, et à quelles conditions

*Inchangé (v3).* On apprend la forme en la consommant → D20 met le lecteur de store en tête. Condition
non négociable : **le registre des écarts s'écrit au fil de l'eau**, jamais reconstitué à la fin
(leçon HANAMI). Fork nommé **par son sujet**, pas par un client. **Contrat de diff préservé.**

*Renfort v4* : la forme d'un profil de gabarit n'est plus une invention — elle est **dérivée de 11 cas
réels**. Son entrée au registre est donc un rattrapage documenté, pas un pari.

### 1.9 La correction sur `depo == "NC"` *(nouveau — v4)*

La v3 écrivait : « *« Non communiqué » sert de proxy pour « France » : un dépositaire simplement non
renseigné bascule le contrat en AV FR. C'est un faux silencieux, pas une approximation.* »

**Le salvage corrige cette lecture.** `references/03-tableau-exhaustif.md` documente que
**NC = « non communicable », pas « non connu »**, et que la déduction est **réglementaire** :

- les AV/Capi de droit **luxembourgeois** doivent nommer une banque dépositaire tierce (*triangle de
  sécurité*) → un dépositaire nommé implique **LU** ;
- les contrats de droit **français**, où l'assureur porte seul la responsabilité bilantielle sans
  dépositaire tiers identifiable, inscrivent **NC** par convention (jamais le nom de l'assureur) →
  implique **FR**.

La règle est donc **sémantiquement fondée**. Le défaut réel est ailleurs, et il est plus étroit :
**« non communicable » et « non renseigné » s'écrivent tous les deux `NC`**, donc une saisie manquante
est indistinguable d'une convention réglementaire. C'est une **homonymie**, pas une erreur de raisonnement.

Conséquence : G4/K4 ne consistent pas à *supprimer* la déduction mais à la **rendre explicite et
distinguable** — domiciliation lue du référentiel des acteurs, `NC` conservé comme **valeur légitime**
signifiant « FR, pas de dépositaire tiers », et une valeur distincte pour « non renseigné » qui, elle,
déclenche une question. (T5, D26)

### 1.10 Signature de gabarit — acquis de l'étude empirique *(nouveau — v4)*

Étude du 27/07 : 11 relevés réels, un sous-agent par document, en parallèle, lecture seule.
**Tout le corpus est en texte natif propre — zéro OCR.**

**a. Trois découvertes de modélisation**

1. **Émetteur ≠ partenaire ≠ nom de fichier.** Les 4 fichiers « Interagyr » recouvrent 2 émetteurs
   (de Pury Pictet Turrettini et Dauphine AM) et « Interagyr » est un **client**. « UAF » est un
   distributeur, l'émetteur est **Spirica**. Pour Nortia et Himalia, l'émetteur **n'apparaît pas dans
   le texte** (identité portée par un logo image). → Identification par **contenu uniquement** (D24),
   et nommage humain une fois au seed quand l'émetteur n'est pas lisible.
2. **La signature est en couches pondérées**, pas un hash :

   | Couche | Source | Coût | Rôle | Limite |
   |---|---|---|---|---|
   | 1 — metadata PDF | `pdfinfo` Creator/Producer | quasi nul | pré-filtre | **sous-discrimine** (JasperReports chez Spirica *et* Himalia) → jamais seule |
   | 2 — ancres boilerplate | chaînes verbatim stables | faible | **cœur discriminant** | tolérance requise aux dates embarquées dans les libellés |
   | 3 — structure | sections ordonnées + en-têtes de colonnes exacts | moyen | confirmation + base d'ancrage | en-têtes wrappés sur 2-3 lignes |

   Validé par les réplicats : Dauphine ×3, Spirica ×2, Wealins ×2 — mêmes couches stables, valeurs
   divergentes.
3. **Le nombre de pages est exclu de toute signature.** Prouvé mortel : Dauphine 3→6 pages, Wealins
   15↔26 pages **pour le même gabarit**. Idem pour les dates, les libellés à date embarquée, les
   libellés de poches dépendant du mandat, la composition de benchmark et le nom du signataire.

**b. Trois aubaines pour le full-auto**

- **ID de template natif** quand il existe : Cardif embarque `<MODELE>Relevé situation
  front-office</MODELE>` + `<TYPE_MODELE>66</TYPE_MODELE>`. Signature parfaite, à exploiter
  opportunistement (champ optionnel du profil).
- **Périodicité discriminée par un libellé, pas par une date** : la date d'arrêté ne discrimine pas
  (un trimestriel peut tomber au 31/12). Le token Cardif est « Cumul des opérations pour l'**année** »
  (→ « pour le mois » / « pour le trimestre »). La granularité *émetteur × gabarit × périodicité* est
  validée.
- **Invariant de contrôle** : plusieurs relevés n'impriment **pas** de ligne total (Himalia, Dauphine)
  → le moteur recalcule et croise. Vérifié : Σ des 9 lignes Himalia = « Épargne atteinte » au centime ;
  Σ des 4 sous-totaux Dauphine = Total portefeuille. Chaque profil stocke son invariant →
  **auto-validation de l'extraction sans humain** (D25). C'est de la rigidité gratuite.

**c. Champs publiés — matrice de capacité** *(consolidé étude + salvage Gronier)*

Le SKILL.md exige `perf_pct` « dès que la source la publie ». Encore faut-il savoir qui publie quoi :

| Émetteur | Perf par ligne | Forme |
|---|---|---|
| de Pury Pictet Turrettini | oui | « RESULTAT YTD », **2ᵉ ligne** = depuis achat |
| Dauphine AM | oui | « +/- % » **hebdomadaire**, pas YTD |
| Nortia | non (dérivable) | plus-value en **€**, % à dériver du gain/PMA |
| UAF (Spirica) / Himalia | oui | format « PRM » |
| Cardif | non | plus-value en € seulement |
| Wealins | **par poche seulement** | pas de perf par ligne |

C'est un champ du profil de gabarit (`champs_publies`), pas une note de prompt. Là où la source ne
publie rien, le comblement par source tierce (O2S en bouche-trou, par ISIN) est un choix **explicite
et tracé**, pas un silence.

**d. Le seed est disponible sans travail neuf**

- **7 profils de gabarit / 11 documents** : PPT mandat coté · Dauphine hebdo (×3) · Spirica AV (×2) ·
  Nortia CTO · Himalia capi · Wealins annuel (×2) · Cardif capi PM.
- **Acteurs Gronier** (31 relevés déjà parsés) : Wealins, Quintet, CA Indosuez, Banque Thaler, Intesa,
  CIC, Cardif, Nortia, UAF, Himalia, Altaroc, SOMNOO, OCA MGM1 ; et côté exemples vendorés du skill :
  UBS, Edmond de Rothschild, Tilvest, De Pury Pictet, Dauphine AM, Prédica, CNP, Generali,
  Cardif Lux Vie.
- **Successions datées déjà documentées** : FC051727 poche FAS *Intesa → CA Indosuez* ; poche FID
  *CIC → Quintet*, clôture au 30/12/2025.
- **Convention de libellé** à porter dans les alias : écrire « De Pury Pictet », jamais « PPT » ni le
  nom complet.

**e. Un problème ouvert de modélisation**

Les **documents composites** : le relevé annuel Wealins est la concaténation de 4 sous-templates
(courrier, information annuelle réglementaire, page Loi Pacte, annexe booklet par FID), chacun avec sa
**pagination locale**, et un footer de fax différent sur la page Loi Pacte. Un PDF = plusieurs
gabarits. Profil composite unique, ou profil par sous-template ? → O11.

### 1.11 Décisions actées

D1–D11 : *voir v1.* D12–D17 : *voir v2.* D18–D21 : *voir v3.* Ajouts du 27/07 (troisième atelier) :

| # | Décision |
|---|----------|
| D22 | **Scission de K.** Le référentiel des **acteurs** (qui/quel rôle) et le référentiel des **gabarits** (quel template) sont deux tables distinctes et chaînées : `gabarit.emetteur` est une clé étrangère vers un code d'acteur. K porte les acteurs, **N** porte les gabarits. |
| D23 | **Trois référentiels, une gouvernance.** Acteurs, gabarits et ISIN logent dans `meta.reference_tables`, partagent le circuit de proposition d'ajout et la cotation mécanique de `confidence`. E5 s'élargit aux trois. |
| D24 | **Identification par le contenu.** L'émetteur d'un document ne se déduit jamais du nom de fichier ni du partenaire commercial. Signature seule. Quand l'émetteur n'est pas lisible en texte (logo), il est nommé par un humain **une fois**, au seed. |
| D25 | **Invariant de contrôle par gabarit.** Chaque profil porte son checksum interne (Σ lignes = tel total, Σ % = 100…). Il auto-valide l'extraction sans intervention humaine et alimente `confidence`. Un invariant en échec est un signal, jamais une correction silencieuse. |
| D26 | **`NC` est une valeur légitime, pas un défaut.** La déduction de domiciliation est réglementaire (triangle de sécurité). Ce qui est corrigé, c'est l'**homonymie** : « non communicable » et « non renseigné » doivent cesser de s'écrire pareil. Le second déclenche une question. |
| D27 | **Le seed des référentiels est constitué à partir de l'existant** (étude de signature + 31 relevés Gronier + référentiels de fait du moteur), jamais reparti de zéro. Éviter le cold-start est un objectif explicite : il conditionne l'adhésion CGP. |

---

## Section 2 — État des lieux du code au 27/07

### 2.1 Ce qui est livré et vérifié

Paquet de référence : merge HANAMI du 27/07 — 118 entrées, `selfcheck` OK, régression **7/7**
(QC 9/9, déterministe), jsdom **24 assertions OK**, audit post-zip sans anomalie.

Acquis moteur, stables et hors chantier : colmap bi-format ; cascade KPI ; Mouvements ; Dietz modifié
(KPI et par contrat, YTD et origine) ; tableau Historique 9 colonnes ; poches reconstruites depuis les
étiquettes de l'onglet Lignes ; non coté scindé Fonds/Titres (`attributes.instrument_type`), multiples
recalculés sur les fonds seuls.

**Référentiel ISIN v0** *(précisé par le salvage)* : `assets/isin_referentiel_v0.csv`, **261 entrées**,
colonnes `isin, label, class_code, geo_code, sri, source, confidence`. Deux sources :
`interagyr_valide` (43 ISIN, confidence **high**, validés Tristan) et `gronier_categories` (212 ISIN,
confidence **medium**, déduits, **sans géographie**). Priorité systématique à `interagyr_valide` ;
15 ISIN communs sans conflit ; 3 exclus pour conflit interne. 177 `geo_code` complétés et validés le
24/07. 13 classes et 5 géographies provisoires.

### 2.2 Dette technique constatée

| # | Constat | Effet |
|---|---|---|
| T1 | **Collision de noms** : `blocs_enabled.historique` = bloc *Rendement annuel*, alors que le tableau *Historique du patrimoine* vit dans `supervision`. | Deux objets, un mot. À renommer avant J. |
| T2 | **Doc périmée** : `manifest.schema.json` annonce 7 blocs « en ordre canonique figé », `BLOCK_ORDER` en compte 9 (`performance_nc` et `historique` intercalés). | Correction documentaire. |
| T3 | **`run_tests.py` écrit en dur dans `/tmp/_reg*`** → `PermissionError` remonté en `CalledProcessError` nu. | Ressemble à une régression moteur. Correctif : `tempfile.mkdtemp()`. |
| T4 | **`jsonschema` 3.2.0** par défaut alors que `store_builder.py` exige ≥ 4.x. | `selfcheck` le détecte. `pip install -U jsonschema`. |
| T5 | *(v4)* **Homonymie `NC`** : « non communicable » (convention réglementaire → FR) et « non renseigné » (donnée manquante) s'écrivent identiquement. | Une saisie oubliée devient silencieusement un contrat français. Cf. D26. |
| T6 | *(v4)* **Couleurs de partenaires non déterministes** : aucun partenaire réel de Gronier n'est dans `DEPO_COLORS` ; ils reçoivent une couleur `FALLBACK` **dans l'ordre de première rencontre**. | Deux runs du même client peuvent produire des donuts de couleurs différentes. Défaut de reproductibilité. |

---

## Section 3 — CDC : les points à tacler

> **Ce document n'est plus l'instrument de suivi** — l'état réel des travaux se lit dans `JOURNAL.md`
> (ce qui a été fait, et pourquoi) et `REPRISE.md` (où reprendre). Les cases ci-dessous ont été
> **recochées le 2026-07-29 après audit** : 20 tâches faites, 7 faites à moitié et annotées comme telles.
> Les décisions actées vont désormais jusqu'à **D42**, alors que la liste du §1.11 s'arrête à **D27** :
> D28–D31 vivent dans `docs/spec_tri_blocs_widgets_2026-07-27.md`, D32–D42 dans
> `docs/roadmap_2026-07-27.md`. *(Cet encadré s'arrêtait à D41 ; D42 — clé d'unicité des gabarits en
> `(emetteur_code, gabarit, valide_depuis)`, périodicité hors matching — a été actée le 29/07 et clôt
> O16.)*

### Chantier A — Excel de structure (CGP-facing, minimal)

- [ ] **A1.** 3 feuilles : **Identité**, **Coté** (1 ligne = 1 contrat, colonne Entité), **Non coté**. ⚠️ *A1 supprime la topologie d'onglets dont dépend le tri actuel : pas livrable sans J.*
- [ ] **A2.** Colonnes coté minimales : entité, nature, banque, gérant, nature de gestion, profil, date d'invest., capital investi, valeur si connue.
- [ ] **A3.** Poches : **jamais saisies**. Colonne optionnelle « Nombre de poches attendu » = contrôle.
- [ ] **A4.** PS : ligne d'ancrage expansée par le skill. Convention anti-double-compte à verrouiller.
- [ ] **A5.** Catégories hors périmètre présentée : acceptées, transitent, ignorées au rendu.
- [ ] **A6.** La colonne « Type (Fonds/Titre) » doit survivre. Les validations de données ne survivent pas à openpyxl — les recréer en natif.
- [ ] **A7.** *(v4)* **Distinguer « non communicable » de « non renseigné »** dans la colonne dépositaire (D26/T5) : valeur `NC` explicite au déroulant, vide = question.

### Chantier B — Extraction & réconciliation

- [ ] **B1.** Un subagent par document PDF (parallélisables, contexte isolé). Sortie = contrat de diff, validé par jsonschema avant écriture.
- [ ] **B2.** Profil skill du contrat de diff : `old_value` = valeur structure ou null ; `source_page` systématique ; `unrecognized_data` alimenté.
- [ ] **B3.** Réconciliation : écart hors tolérance ou confidence low → **AskUser**, décision tracée. Jamais de correction silencieuse.
- [ ] **B4.** Pièges à encoder — *amendé v4 : ils ne vivent plus dans le prompt mais dans le profil de gabarit (N)*. Le prompt reçoit les hints du profil identifié.
- [ ] **B5.** **Capter la référence de rattachement énoncée par le relevé** (D14) : contrat X, et le cas échéant la poche. Champ de sortie obligatoire du subagent.
- [ ] **B6.** **Coter la `confidence` depuis les référentiels** (K et N) : couple exact = high, alias = medium, inconnu = low. Aujourd'hui au jugement du subagent, donc non reproductible.
- [ ] **B7.** **Ne pas déformer le contrat de diff** — point de contact avec `validation_app`. Les enrichissements du fork vont dans le forme-store.
- [ ] **B8.** *(v4)* **Vérifier l'invariant de contrôle du gabarit** avant d'émettre le diff (D25). Invariant OK → pas de question. En échec → signal, jamais correction.
- [ ] **B9.** *(v4)* **Lire `champs_publies` du profil** pour savoir si `perf_pct` est attendu, dérivable (Nortia : € → % via PMA), disponible par poche seulement (Wealins) ou absent (Cardif). Le comblement par source tierce devient explicite et tracé, plus un silence.
- [ ] **B10.** *(v4)* **Étape d'identification préalable** : signature → profil, avant tout dispatch de subagent. En cas de drift, ré-analyse et message d'attente au CGP.

### Chantier C — Pivot forme-store & extensions de schéma

*Ces extensions ne passent plus par le canal Code : décidées dans le fork, posées à leur position canonique présumée, consignées au registre (D19, D21).*

- [ ] **C1.** Le skill émet le dict forme-store. Ids `tmp_`, `_provisional_ids: true`.
- [ ] **C2.** Extensions à poser dans le fork :
  - `payload.attributes.lines: [{isin, label, value, class, geography, sri, pocket}]` et `pockets: [...]` sur `financier_cote` ;
  - `entry_ref` sur les entrées `mouvements` ;
  - `unit` sur `meta.valuations` ; valuations par `position_id` ;
  - attributs PS dans le payload.
- [ ] **C3.** Discipline : codes `reference_tables` partout ; absence ≠ null ; ordre des listes stable.
- [ ] **C4.** Fixer les ids provisoires de Gronier et INTERAGYR. *Prérequis de L1.* ⚠️ *À moitié : Gronier est fait (`tmp_fc_001..007`, `tmp_nc_001..008`, `_provisional_ids: true` — cf. `reference/gronier_T2/client.store.json`), **INTERAGYR est absent** — aucun store n'existe. L1 est donc ordonnançable, **A9 ne l'est pas**.*
- [ ] **C5.** **Créer `meta.reference_tables`** dans `store_client.schema.json` — position canonique (clé déjà nommée côté infra), inscrite au registre. *Amendé v4 : porte les **trois** tables (D23).*
- [ ] **C6.** **Loger la donnée du bloc Contexte.** Invention réelle, à motiver au registre (O7).
- [ ] **C7.** **Identifiant stable de poche** (D16), distinct du libellé. Invention réelle, à motiver.

### Chantier D — Writer & Excel de transition

*Le writer sort du chemin de rendu (D20). Utile, mais moins urgent.*

- [ ] **D1.** Vendorer `json_to_excel.py` (asset). Gouvernance : O4.
- [ ] **D2.** Compléter avec les feuilles v5 absentes du legacy : Lignes classées, Mouvements, PS enrichis, poches.
- [ ] **D3.** Rectifications CGP repassent par le circuit diff, jamais d'écriture directe dans le pivot.
- [ ] **D4.** Dossier de run archivé : JSON forme-store + diffs + Excel + HTML. Rejouable.

### Chantier E — Référentiel ISIN *(table C des trois)*

- [x] **E1.** Asset CSV `isin → {label, class_code, geo_code, sri, source, confidence}`. **Livré v0, 261 entrées.**
- [x] **E2.** Amorce Gronier + INTERAGYR. *177 geo_code validés CGP le 24/07.*
- [ ] **E3.** Mécanisme « propositions d'ajout » : ISIN inconnus → diff à valider (Tristan) avant intégration. ⚠️ *À moitié : le circuit d'adjudication existe côté store des référentiels (`cible_t` à quatre valeurs, outils `ref_propose` / `ref_arbitrer`, table `adjudications`), mais il **n'a aucun appelant** — `grep -rn 'ref_propose' skill/` ne rend rien. Il manque le producteur de propositions dans le skill.*
- [ ] **E4.** Cible : table du datahub dont l'asset est le snapshot.
- [ ] **E5.** **Mutualiser le circuit de proposition entre les trois référentiels** (ISIN, acteurs K3, gabarits N5). Un seul mécanisme. *Élargi v4 (D23).* ⚠️ *À moitié : la mutualisation est acquise côté store (`cible_t` = `acteur|succession|gabarit|isin`, `nature_adj_t` couvrant les trois natures de N6), mais côté skill rien n'appelle ce circuit — même constat que E3.*
- [ ] **E6.** *(v4)* Compléter les `geo_code` manquants de la source `gronier_categories` (212 ISIN sans géographie, confidence medium). ⚠️ *À moitié, et seulement sur le décompte : le manque réel est de **44 ISIN sur 261**, pas 212 — la validation CGP du 24/07 en avait déjà complété 173 (cf. `JOURNAL.md`, entrée « A4 »). Aucune complétion n'a été faite dans le fork : `skill/assets/isin_referentiel_v0.csv` est **inchangé depuis l'ouverture** (md5 identique à `BASELINE_MD5.txt`).*

### Chantier F — Expositions géographiques

*Inchangé.* Hiérarchie de sources (titres vifs → siège ; ETF → indice du nom ; fonds actifs →
ventilation gestionnaire, sinon zone dominante, sinon **non tagué**) ; donut de **couverture** ;
V1 à une zone par ligne, look-through différé.

### Chantier G — Moteur (périmètre volontairement minimal)

- [ ] **G1.** **Inliner Chart.js + polices** (base64, ~350 Ko/doc). **Avant le premier PUT en base.**
- [x] **G2.** Colmap, cascade KPI, Mouvements, agrégations : livrés et testés.
- [x] **G3.** Corriger T1 et T2 **avant** d'écrire `store_to_manifest`. *(fait le 2026-07-28 — cf. `JOURNAL.md`, entrées « A2 » et « Suppression du bloc Rendement annuel » ; T1 clos autrement que par un renommage — le bloc a été renommé puis **supprimé**, `BLOCK_ORDER` passé de 9 à 8 ; T2 corrigé dans `manifest.schema.json` avec contrôle croisé des noms dans les deux sens ; régression 7/7, QC 9/9, déterministe)*
- [ ] **G4.** Sortir `envelope()` de sa déduction implicite → domiciliation lue du référentiel des acteurs. *Amendé v4 : **conserver** la règle réglementaire, lever l'homonymie `NC` (D26).*
- [ ] **G5.** *(v4)* **Rendre les couleurs de partenaires déterministes** (T6) : palette assignée depuis le référentiel, pas dans l'ordre de première rencontre.

### Chantier H — Couture & intégration infra

- [x] **H1.** SKILL.md : spécifier le **double mode d'entrée** (D6). *(constaté le 2026-07-29 — `skill/SKILL.md` §0 « Mode d'entrée : à détecter EN PREMIER », quatre signaux dont le `{client}.json` conforme à `store_client.schema.json` ; acquis **avant** le fork, le fichier étant inchangé vs `BASELINE_MD5.txt`)*
- [ ] **H2.** Intérim : PJ HTML + JSON à l'entrée de l'atelier.
- [ ] **H3.** Cible : MCP datahub — GET client, PUT/PATCH reporting. Jamais d'accès direct.
- [ ] **H4.** À spécifier au retour : table `reportings` (client_id, date, mode, html, source_json, generated_by).
- [ ] **H5.** Débranchement final : suppression du module d'agrégation, zéro impact aval.
- [ ] **H6.** *(v4)* **L'API partenaire comme 3ᵉ producteur** du contrat de diff, diffs pré-labellisés `validated_by: <partenaire>` (source autoritaire, pas un email CGP). Le référentiel des gabarits **reste en lifeline** si le flux casse. Horizon ≥ 3 mois.

### Chantier I — Gouvernance & méthode

- [ ] **I1.** Toute évolution se définit d'abord comme une forme dans le dict store. ⚠️ *À moitié : la discipline a tenu sur l'invariant A5 — schéma amendé et testé avant d'écrire le lecteur (`JOURNAL.md`, « Implémentation — invariant A5 ») —, mais **onze formes décidées restent non posées** — la liste « À inscrire dès qu'on y touche » du registre, soit **neuf manques** (`MQ1`–`MQ8` et `MQ10`, renommés depuis `M…` le 29/07) **plus `C6` et `C7`**, et non « M1 à M11 » comme cette annotation le disait : `MQ9` a été tranché en faveur du manifeste et `MQ11` est un défaut de remplissage, ni l'un ni l'autre n'est une forme à poser. Et deux décisions se définissent **hors** du dict store : `tri_decisions` au manifeste (R6) et le contexte de marché en table séparée (D39).*
- [x] **I2.** Canal formel **suspendu pendant le fork** (D18), réactivé en un passage alimenté par le registre. *(fait le 2026-07-28 — aucune sollicitation de Code depuis l'ouverture du fork ; les extensions sont posées unilatéralement et consignées au fil de l'eau, R1–R8, et `REGISTRE_ECARTS.md` s'annonce lui-même comme l'addendum du jour du retour (M7))*
- [ ] **I3.** Banc d'essai de bout en bout : **INTERAGYR**.
- [ ] **I4.** Ordre d'exécution : *voir section 4.*

### Chantier J — Tri des blocs

- [ ] **J1.** Écrire `store_to_manifest.py` : entités depuis le store, `blocs_enabled` calculé, mêmes garde-fous que `assemble.validate`.
- [x] **J2.** **Tableau de faisabilité** bloc par bloc. Cas à trancher : `supervision`, `performance`, `contexte` (aucun logement, C6). *(fait le 2026-07-27 — `docs/spec_tri_blocs_widgets_2026-07-27.md` §2 : neuf blocs, nature et règle d'apparition pour chacun, `supervision` et `performance` tranchés en faisabilité ; le logement de `contexte` a été tranché depuis par D39, table séparée par période. Attention : cette spec est **partiellement périmée**, elle décrit le bloc `rendement_annuel` supprimé depuis)*
- [ ] **J3.** Séparer l'intention : `mode` porte `repartition`/`exhaustif` ; expliciter les `True` en dur.
- [ ] **J4.** Appliquer D17 : un bloc sans donnée est désactivé, jamais rendu en squelette.
- [ ] **J5.** `excel_to_manifest.py` conservé en legacy, sans évolution. ⚠️ *À moitié : le fichier est bien conservé en legacy, mais « sans évolution » est **faux** — son md5 diffère de `BASELINE_MD5.txt` (il apparaît en MODIFIÉ dans `verifier_empreinte.sh`). Il a été touché deux fois : renommage D31 puis retrait de l'émission du bloc supprimé (`JOURNAL.md`, « A2 » et « Suppression du bloc Rendement annuel »). La règle est à reformuler, ou l'écart à assumer.*
- [ ] **J6.** Ne pas toucher `assemble.compute_layout`.

### Chantier K — Référentiel des acteurs *(ex-« couples partenaires », recadré v4)*

- [x] **K1.** Contenu : **code canonique**, **rôle** (assureur / dépositaire / gérant / plateforme-grossiste — Nortia est un grossiste, pas un assureur), **alias verbatim des PDF**, domiciliation, **successions datées**, **couples admissibles**. *(fait le 2026-07-28 — `infra/ddl_referentiels_v0.sql` (table `acteurs`, `role_acteur_t` à quatre valeurs) et `seed/acteurs.json` : 17 acteurs avec `code`, `role`, `alias`, `domiciliation`, `est_depositaire_tiers` ; successions dans `seed/successions.json` (K5). Réserve : les **couples admissibles** ont leur logement — le `payload` JSONB, documenté comme tel dans le DDL — mais aucun n'est peuplé au seed)*
- [ ] **K2.** Logement : `meta.reference_tables` (C5), inscrit au registre.
- [ ] **K3.** Circuit « acteur inconnu » : `unrecognized_data` + proposition à valider, mutualisé (E5).
- [ ] **K4.** Retirer les référentiels de fait du moteur : `DEPO_COLORS`, `ENV_COLORS`, déduction de `envelope()` (G4). Les couleurs restent une préoccupation de rendu ; la liste des acteurs ne vit plus dans `p2_fill.py`.
- [x] **K5.** Les successions doivent réconcilier un relevé antérieur à un changement (cas Wealins FC051727 / relevé 2024). **Seed disponible** : FAS *Intesa → CA Indosuez* ; FID *CIC → Quintet* (clôture 30/12/2025). *(fait le 2026-07-28 — `seed/successions.json`, deux successions datées avec `predecesseur_code` / `successeur_code` / `date_cloture` et leur contexte de poche, table `acteur_successions` au DDL ; cf. `JOURNAL.md`, entrée « A4 ». Les deux `date_effet` restent à `null`, non établies par le relevé, plutôt que devinées)*
- [x] **K6.** *(v4)* **Seeder depuis l'existant** (D27) : acteurs Gronier + acteurs des exemples vendorés + `DEPO_COLORS`. Porter les conventions de libellé (« De Pury Pictet », jamais « PPT »). *(fait le 2026-07-28 — 17 acteurs seedés depuis les 31 relevés Gronier, les exemples vendorés et les référentiels de fait du moteur ; conventions de libellé portées dans `payload.convention` ; deux identités non lisibles laissées explicites plutôt que devinées — `himalia_emetteur_a_confirmer` en confiance `low`. Cf. `JOURNAL.md`, entrée « A4 »)*
- [ ] **K7.** *(v4)* **Trancher l'ambiguïté Thaler / CA Indosuez** : le salvage ne permet pas de dire s'il s'agit de la même entité ou de deux repreneurs distincts de la poche ex-Intesa. Question au CGP, pas une déduction. ⚠️ *À moitié : la question est **inscrite**, pas tranchée — `payload.question_ouverte` sur `ca_indosuez` et `banque_thaler` dans `seed/acteurs.json`, et sur la succession *Intesa → CA Indosuez* dans `seed/successions.json`. Elle attend un AskUser au CGP. Correction du 29/07 : cette annotation donnait les deux acteurs en confiance `low` — ils sont en **`medium`** dans `seed/acteurs.json`, comme les autres établissements repris du salvage. Le seul acteur du seed en `low` est `himalia_emetteur_a_confirmer`, dont c'est le nom même qui porte la réserve.*

### Chantier L — Lecteur de forme-store *(colonne vertébrale)*

- [ ] **L1.** Produire un **forme-store de référence** pour un client réel (Gronier, déjà réconcilié) et en faire la fixture. Prérequis : C4.
- [ ] **L2.** Écrire le lecteur : du forme-store vers les structures que `p2_fill` consomme du classeur. Le contrat de sortie existe déjà — le moteur ne change pas.
- [ ] **L3.** **Équivalence de rendu prouvée** : même client, rendu depuis l'Excel ≡ rendu depuis le store (ou écarts explicables un par un). Mesure accessoirement la perte réelle de la projection Excel.
- [ ] **L4.** Étendre la régression : les 7 fixtures sont des classeurs ; prévoir leur pendant forme-store.
- [ ] **L5.** Chaque extension de C2 est livrée **lecteur d'abord** : on la consomme avant de la déclarer stable.

### Chantier M — Fork, registre et convergence

- [x] **M1.** Créer le fork, nommé **par son sujet**, à partir du paquet mergé du 27/07. *(fait le 2026-07-28 — `FORK-Cowork`, ouvert depuis le paquet `reporting-fo-rhetores-alt` (merge HANAMI du 27/07), 118 fichiers, caches exclus ; cf. `JOURNAL.md`, « Ouverture du fork »)*
- [x] **M2.** Créer le **registre des écarts** dès le premier commit : clé, position, forme, justification, statut (rattrapage / invention réelle), et ce qu'il faudrait pour la faire bénir. *(fait le 2026-07-28 — `REGISTRE_ECARTS.md` ouvert avec R1–R6 et les cinq champs prescrits. Réserve levée le 2026-07-28 : le dépôt **est versionné** depuis ce jour-là, « dès le premier commit » a donc désormais un support ; `BASELINE_MD5.txt` + `JOURNAL.md` + `CHECKSUMS.json` restent utiles mais ne sont plus le seul filet)*
- [x] **M3.** Tenir le registre **au fil de l'eau** (D21). Aucune clé posée sans son entrée. *(fait le 2026-07-28 — R7 inscrite dans le même mouvement que la décision O11, et les manques MQ1–MQ11 de l'inventaire du lecteur consignés le jour où ils ont été constatés ; cf. `JOURNAL.md`, entrées « O11 » et « A1 — l'inventaire avant le code »)*
- [x] **M4.** Maintenir le suffixe `-skill` sur `schema_version`. *(fait le 2026-07-28 — `skill/pipeline/store_client.schema.json` : `schema_version` reste `const: "2.0-skill"`, y compris après l'amendement A5 qui a modifié le fichier)*
- [x] **M5.** Préserver le contrat de diff intact (B7). *(fait le 2026-07-28 — `skill/pipeline/diff_contract.schema.json` a un md5 **identique** à `BASELINE_MD5.txt` ; il n'apparaît pas dans les MODIFIÉS de `verifier_empreinte.sh`, où onze fichiers le sont)*
- [x] **M6.** Protocole de merge, tiré de HANAMI : md5 base→cible, écriture par copie, vérification après chaque écriture, audit fichier par fichier. À écrire **avant** d'en avoir besoin. *(fait le 2026-07-28 — `README.md` §« Protocole de merge (M6) — écrit avant d'en avoir besoin », cinq étapes ; outillé par `BASELINE_MD5.txt`, `verifier_empreinte.sh` et `regenerer_checksums.py`. Éprouvé en vrai lors du retrait du snapshot client, où il a fait apparaître le bon écart)*
- [ ] **M7.** Au retour vers Code : l'addendum **est** le registre, relu et ordonné. Un passage, un bump.

### Chantier N — Référentiel des gabarits *(nouveau — v4)*

- [x] **N1.** **Schéma d'un profil** (dérivé de 11 cas réels) : *(fait le 2026-07-28 — `infra/ddl_referentiels_v0.sql` §5, table `gabarits`, colonnes `signature` et `extraction_hints` JSONB, `champs_publies`, `invariant_controle`, `template_id_natif`, `emetteur_lisible`, `provenance`/`confiance` ; forme consignée au registre en R4, et le nombre de pages est bien hors matching. **Clé d'unicité `(emetteur_code, gabarit, valide_depuis)` depuis D42** — la réserve O15 inscrite ici, « `gabarit` doit nommer le template et non sa fonction, sans quoi la contrainte d'unicité est instable », est **réglée** : la fenêtre de validité entre dans la clé, deux versions d'un même gabarit coexistent, et un nommage imparfait ne peut plus faire écraser un profil. `periodicite` reste une colonne, mais informative et hors matching)*
  ```
  profil := {
    cle:      { emetteur_code → FK acteurs (K), gabarit, periodicite },
    signature: {
      metadata_pdf:      { creator, producer },     // couche 1, jamais seule
      template_id_natif: str?,                      // ex. Cardif TYPE_MODELE 66 — court-circuit
      ancres_texte:      [...],                     // couche 2, cœur discriminant
      marqueurs_structure:[...],                    // couche 3, sections + colonnes
      token_periodicite: str,                       // ex. "pour l'année"
      n_pages_indicatif: "plage large, HORS matching"
    },
    extraction_hints: { pieges[], ancrage_tableaux[], format_numerique{} },
    champs_publies:   { perf_par_ligne: oui|non|par_poche|derivable, forme },
    invariant_controle: str,                        // D25
    gouvernance:      { version, provenance: seed|auto_derive|valide_admin, confiance }
  }
  ```
- [x] **N2.** **Seeder les 7 profils** de l'étude (PPT · Dauphine ×3 · Spirica ×2 · Nortia · Himalia · Wealins ×2 · Cardif) — empreintes complètes disponibles dans `etude_signature_gabarits_2026-07-27.md`. *(fait le 2026-07-28, révisé le 2026-07-29 — `seed/gabarits.json` porte désormais **9 profils** et non 7 : le corpus étendu a établi que **Wealins expose deux gabarits réels** (`releve_annuel_capi_lux` et `situation_trimestrielle_capi_lux`) et que **Cardif en a produit deux successifs**, portés par deux fenêtres de validité (D42). Unicité contrôlée avant écriture, toute référence d'acteur résolue ; promus dans `seed/referentiels.json` et `seed/seed.sql`, et **chargés en base** (17 acteurs, 2 successions, 9 gabarits, 261 ISIN). Cf. `JOURNAL.md`, entrée « A4 ». La réserve inscrite ici — « le profil Wealins est encore composite à segments, ce que D38 contredit, O11 est rouvert » — **est levée** : **O11 est clos par la donnée**, les 7 documents Wealins du corpus étant composites 7 fois sur 7, donc le profil annuel reste bien un composite à segments, conformément à R7 dans sa rédaction d'origine)*
- [ ] **N3.** **Migrer les pièges hors du prompt** : §2.b du SKILL.md → `extraction_hints` du profil. Dédoublonne et rend l'extraction pilotée par la donnée (amende B4).
- [ ] **N4.** **Matcher en couches** avec biais paranoïaque assumé : en cas de doute, signaler le drift. Asymétrie des coûts — une ré-analyse coûte du Haiku, une erreur silencieuse coûte un reporting client faux. **Exclure le nombre de pages du matching.**
- [ ] **N5.** **Self-healing** : drift → ré-analyse LLM complète → profil re-dérivé, **appliqué à ce run**, **proposé** à l'arbitrage admin (jamais écrit d'autorité). Circuit mutualisé (E5).
- [x] **N6.** **Trois natures de proposition** à distinguer dans le contrat : nouvel émetteur / nouveau gabarit d'un émetteur connu / variante de périodicité. Elles n'appellent pas le même arbitrage. *(fait le 2026-07-28 — `infra/ddl_referentiels_v0.sql` : `nature_adj_t` porte `nouvel_emetteur`, `nouveau_gabarit`, `variante_periodicite`, plus les cas des autres tables et `mise_a_jour` ; la colonne `nature` est `NOT NULL` sur `adjudications` et indexée avec `cible`. Le producteur de propositions reste à écrire côté skill — cf. E3/E5)*
- [x] **N7.** **Émetteur non lisible en texte** (Nortia, Himalia — logo image) : nommage humain une fois au seed ; au runtime, identification par signature seule (D24). *(fait le 2026-07-28 — colonne `emetteur_lisible` au DDL, et `emetteur_lisible: false` sur les profils `nortia` et `himalia_emetteur_a_confirmer` dans `seed/gabarits.json` ; Nortia nommé par une source fiable, l'émetteur Himalia laissé en code explicite `_a_confirmer` avec sa question ouverte plutôt qu'un nom plausible inscrit. Cf. `JOURNAL.md`, entrée « A4 »)*
- [ ] **N8.** Calibrer le seuil de match sur le corpus réel avant de figer : les réplicats (Dauphine ×3, Spirica ×2, Wealins ×2) sont le banc d'essai des faux positifs/négatifs.

---

## Section 4 — Chemin critique

| Jalon | Contenu | Pourquoi ici |
|---|---|---|
| **① Fork et registre** | M1–M4. Quelques minutes. | Rien de ce qui suit ne doit être posé hors registre |
| **② Lecteur de forme-store** | L1–L4, C4 en prérequis. | La colonne vertébrale (D20). Tant que le moteur ne consomme pas le pivot, les extensions se décident à l'aveugle |
| **③ Hygiène préalable** | G3 (T1, T2). | Avant d'écrire le mapping, sinon il hérite de la confusion |
| **④ Tri des blocs** | J1–J6. | Manifeste et données viennent enfin du même endroit |
| **⑤ Référentiels — logement et seed** | C5 (les 3 tables), K1–K7, **N1–N2**, E3/E5/E6. | *Amendé v4* : un logement, un circuit, et le **seed disponible sans travail neuf** (D27) |
| **⑥ Identification et hints** | **N3–N8**, B10, B4 amendé, G4/G5 (T5/T6). | Débloqué par ⑤ : le profil doit exister avant de primer l'extraction |
| **⑦ Structure 3 feuilles** | A1–A7. | Débloqué par ④ |
| **⑧ Extraction & profil diff** | B1–B9, `confidence` cotée depuis K et N, invariants. | Débloqué par ⑤ et ⑥ |
| **⑨ Writer & feuilles v5** | D1–D2. | Hors chemin de rendu : urgence retombée |
| **⑩ Run INTERAGYR de bout en bout** | I3. | Éprouve les formes sur trois versions de vérité |
| **⑪ Retour vers Code** | M7, H4. | Les formes sont éprouvées ; l'addendum s'écrit tout seul |

**Ce que la v4 déplace** : ⑤ gagne le seed et le logement des trois tables ; un jalon ⑥ apparaît
(identification/hints) entre les référentiels et l'extraction, parce que **le profil doit exister
avant de pouvoir primer un subagent**. L'extraction descend en ⑧.

**Ce que ça gagne** : aucune attente externe, et le seed évite le cold-start — donc l'adhésion CGP
n'est pas hypothéquée par un premier run lent.

**Ce que ça coûte** : une dette explicite portée par le registre, et un merge à faire un jour.

---

## Section 5 — Points ouverts

| # | Sujet | État |
|---|-------|------|
| O1 | Propriété de l'egress à terme | *D20 fait pencher vers « le skill consomme le JSON »* |
| O2 | UUID vs autre pour les ids définitifs | Différé, non bloquant (D7) |
| O3 | Drop de l'export Excel dans la validation_app | Sensible politiquement |
| O4 | Gouvernance du vendoring `json_to_excel` | À trancher avec Code, au retour |
| O5 | Ids provisoires Gronier / INTERAGYR | **Bloquant** : prérequis de L1 (C4) |
| ~~O6~~ | ~~Divergence des sources de rendu~~ | **Tranché par D20** |
| O7 | Bloc Contexte : alimenté depuis le store (C6) ou entrée de run ? | Non tranché. Conditionne J2 |
| O8 | Seuils de pertinence (D13) : à partir de quelle part un bloc PE se justifie-t-il ? Seuils codés ou AskUser ? | Non tranché |
| O9 | Jusqu'où le fork peut aller avant que le merge ne devienne déraisonnable ? Critère d'arrêt (N entrées au registre ? extension touchant le contrat de diff ?) | Non tranché |
| O10 | *(v4)* **Plafond de parallélisme.** Gronier : 31 relevés en **3 vagues de ~6 min**. La latence d'une vague semble indépendante de sa taille, mais le nombre de vagues est contraint. Quelle est la taille de vague optimale, et le gain attendu pour un client à 10-20 documents ? À mesurer, pas à supposer. | Nouveau |
| O11 | *(v4)* **Documents composites.** Le relevé annuel Wealins = 4 sous-templates concaténés (paginations locales distinctes, footer différent sur la page Loi Pacte). Profil composite unique, ou profil par sous-template avec match par segments ? | Nouveau |
| O12 | *(v4)* **Comblement par source tierce.** Cardif et Wealins ne publient pas de perf par ligne ; Gronier a comblé via O2S par ISIN, alors que O2S est rétrogradé en bouche-trou pour qualité insuffisante. Assume-t-on ce comblement, ou une perf par ligne absente reste-t-elle absente ? | Nouveau |

---

*Sources v4 : CDC v3 (27/07) ; `etude_signature_gabarits_2026-07-27.md` (11 relevés, 11 sous-agents) ;
`checkpoint_store_partenaire_2026-07-25.md` ; salvage de la session « Reporting skill (Gronier) »
(31 relevés, référentiel ISIN v0, `p2_fill.py`, `references/03-tableau-exhaustif.md`).*
